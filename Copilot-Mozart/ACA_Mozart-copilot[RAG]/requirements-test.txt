# ═══════════════════════════════════════════════════════════════════
# Mozart RAG - Test Requirements (CI + Local)
# ═══════════════════════════════════════════════════════════════════
# Single source of truth for all test dependencies.
# Used by: .github/workflows/docker-build.yml, golden-tests.yml
# Install: pip install -r requirements-test.txt
# ═══════════════════════════════════════════════════════════════════

# --- Production dependencies (must match Docker/requirements_light.txt) ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.3
pydantic-settings>=2.1.0
httpx>=0.26.0
slowapi>=0.1.9
google-generativeai>=0.8.0
python-dotenv>=1.0.0
PyJWT>=2.8.0
supabase>=2.0.0
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2

# --- Test-only dependencies ---
pytest>=7.0.0
pytest-asyncio>=0.21.0
