# ═══════════════════════════════════════════════════════════════════
# Mozart RAG - Lightweight Requirements (Fast Build)
# ═══════════════════════════════════════════════════════════════════
# Use Google AI only (no Vertex AI - saves ~10 minutes build time)
# ═══════════════════════════════════════════════════════════════════

# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.3
pydantic-settings>=2.1.0

# HTTP Client
httpx>=0.26.0

# Rate Limiting (Security)
slowapi>=0.1.9

# LLM Provider - Google AI Only (simpler, faster to install)
google-generativeai>=0.8.0

# Utilities
python-dotenv>=1.0.0
google-cloud-logging>=3.0.0
google-cloud-core>=2.4.1


# Supabase - Stateful Intelligence (v2+ = HTTP REST only, no websocket)
supabase>=2.0.0

# Vector Database - FAISS (lightweight)
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2

