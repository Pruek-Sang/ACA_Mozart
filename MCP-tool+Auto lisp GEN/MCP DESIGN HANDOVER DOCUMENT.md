# 🏛️ ACA_Mozart: Architectural Handover & Status Report
**Date:** December 10, 2025
**Author:** Architecta (AI System Architect)
**To:** Future AI Agents / Collaborators

---

## 1. 🌟 The Grand Vision (เรากำลังทำอะไร?)
เรากำลังสร้าง **"ACA_Mozart"**: ระบบออกแบบไฟฟ้าอัตโนมัติ (Automated Electrical Design System) ที่ผสานพลังของ **AI (RAG)** เข้ากับ **Engineering (MCP)**

### 🧬 Core Philosophy: "Neuro-Symbolic Architecture"
เราไม่ได้สร้างแค่ Chatbot แต่เราสร้าง **"RAG-Driven Engineering Pipeline"**:
1.  **Right Brain (Neuro):** ใช้ AI (Gemini + RAG) แปลความต้องการมนุษย์ (Thai NLP) -> เป็น JSON Spec
2.  **Left Brain (Symbolic):** ใช้ Python (Pandapower) คำนวณวิศวกรรมตามมาตรฐาน (NEC/EIT) -> สร้างแบบ AutoCAD (.lsp)

---

## 2. 🏗️ System Architecture (โครงสร้างระบบ)

### Service 1: Mozart RAG (Port 8080)
*   **Role:** "The Translator" (ล่าม)
*   **Tech:** FastAPI + Google Gemini + FAISS
*   **Input:** "ออกแบบห้องนอน 1 ห้อง" (Thai Text)
*   **Output:** `ProjectInputSpec` (JSON) ที่ระบุ Device Code, Room Type
*   **Key Feature:** ข้อมูลความรู้ (Vector DB) ถูกฝัง (Baked-in) มาใน Docker Image เลย (ไม่ต้อง Ingest ใหม่)

### Service 2: MCP Core v2 (Port 5001)
*   **Role:** "The Engineer" (วิศวกร)
*   **Tech:** FastAPI + Pandapower + NumPy
*   **Input:** `ProjectInputSpec` (JSON) จาก RAG
*   **Output:** `DesignResult` (JSON) + `AutoLISP Code` (.lsp)
*   **Pipeline:**
    1.  `load_calculator`: คำนวณโหลด (Watts)
    2.  `wire_sizer`: เลือกสายไฟ (sq.mm.)
    3.  `breaker_selector`: เลือกเบรกเกอร์ (Amp)
    4.  `autolisp_generator`: **(จุดที่กำลังจะแก้)** แปลงผลคำนวณเป็น Code วาดรูป

---

## 3. ✅ Current Status (ทำถึงไหนแล้ว?)

### Deployment (เสร็จสมบูรณ์ 100%)
*   **Local Machine:** รันบนเครื่อง `builder-Pruek` ได้แล้ว
*   **Docker:** ใช้ Image จาก Docker Hub (`acatest01`)
*   **Config:** ใช้ `.env` ที่ Root, `docker-compose.prod.yml` ที่ Root
*   **Sync:** Code ในเครื่อง Local ตรงกับ GitHub (`prueksang-web/ACA_Mozart-clone`) เป๊ะ

### Development (สิ่งที่ต้องทำต่อ)
*   **Focus:** เรากำลังจะรื้อระบบ **AutoLISP Generator** (`mcp_core_v2/core/autolisp_generator.py`)
*   **Problem:** Logic การวางตำแหน่ง (Geometry) ปัจจุบันมัน "โง่" (เรียงยาวเป็นตับ `i * 40`)
*   **Goal:** เปลี่ยนไปใช้ Logic ที่ฉลาดขึ้น (Smart Placement) โดยรับข้อมูลไฟฟ้า (Watts, Wire Size) จาก `ResultBuilder` เหมือนเดิม แต่คำนวณ X,Y ใหม่ให้สวยงาม

---

## 4. 🗺️ File Map (แผนที่ขุมทรัพย์)

*   **`docker-compose.prod.yml`**: พระเอกตัวจริงในการรันระบบ
*   **`mcp_core_v2/pipeline.py`**: ผู้คุมกฎ (Orchestrator) ที่เรียกใช้งานทุก Module
*   **`mcp_core_v2/core/result_builder.py`**: คลังข้อมูลไฟฟ้า (Data Hub) ที่เราจะดึงไปใช้
*   **`mcp_core_v2/core/autolisp_generator.py`**: **(เป้าหมายการโจมตี)** ไฟล์ที่เราจะเข้าไปผ่าตัดแก้ Logic การวาด

---

*Report Generated by Architecta - "May the Code be with you."* 💃✨
