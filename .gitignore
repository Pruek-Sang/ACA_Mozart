# Secrets - NEVER commit
.env
.env_ACA
Docker/.env_ACA
*.key
*credentials*.json

# Python cache
__pycache__/
*.pyc

# Note: vector_db/ is NOT ignored - it's pre-built and committed
# This allows Local and Docker to use pre-ingested knowledge
